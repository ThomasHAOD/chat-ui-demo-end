import React from 'react';
import classes from './Header.module.scss';

const Header = () => (
  <header className={classes.Header}>
    <h1>Greyscale Chat</h1>
  </header>
);

export default Header;
